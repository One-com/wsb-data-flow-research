/* @flow */

import React from 'react';
import { PropertiesPanelContainerStyle } from './styles';

type Props = {};

export const PropertiesPanelContainer = (props: Props) => (
  <div style={PropertiesPanelContainerStyle} />
);
